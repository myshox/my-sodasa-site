# 🚀 部署指南 - 網站優化版

> 如何將優化後的網站部署到線上

---

## 📦 **部署前準備**

### **1. 準備 PWA 圖標** ⭐ **最重要**

您需要準備以下圖標檔案：

#### **必須的檔案**
- `icon-192.png` (192x192 像素)
- `icon-512.png` (512x512 像素)
- `og-image.jpg` (1200x630 像素，用於社群分享)

#### **如何快速製作？**

**方法 1：使用線上工具（推薦）**
1. 訪問：https://www.pwabuilder.com/imageGenerator
2. 上傳您的 LOGO（建議至少 512x512）
3. 點擊 "Generate"
4. 下載所有圖標
5. 放到網站根目錄

**方法 2：臨時文字圖標**
如果暫時沒有 LOGO，我可以幫您生成一個簡單的文字圖標：
```html
<!-- 儲存為 icon-generator.html，用瀏覽器開啟 -->
<!DOCTYPE html>
<html>
<head><meta charset="UTF-8"><title>圖標生成器</title></head>
<body>
<canvas id="c" width="512" height="512"></canvas>
<button onclick="download(192)">下載 192x192</button>
<button onclick="download(512)">下載 512x512</button>
<script>
const c = document.getElementById('c');
const ctx = c.getContext('2d');
ctx.fillStyle = '#fdcb6e';
ctx.fillRect(0, 0, 512, 512);
ctx.fillStyle = '#1c1917';
ctx.font = 'bold 180px "Microsoft JhengHei"';
ctx.textAlign = 'center';
ctx.textBaseline = 'middle';
ctx.fillText('蘇打', 256, 200);
ctx.font = 'bold 140px "Microsoft JhengHei"';
ctx.fillText('石器', 256, 340);

function download(size) {
    const tempCanvas = document.createElement('canvas');
    tempCanvas.width = size;
    tempCanvas.height = size;
    const tempCtx = tempCanvas.getContext('2d');
    tempCtx.drawImage(c, 0, 0, size, size);
    const link = document.createElement('a');
    link.download = `icon-${size}.png`;
    link.href = tempCanvas.toDataURL();
    link.click();
}
</script>
</body>
</html>
```

### **2. 更新網址**

在以下檔案中，將所有 `https://soda-stone.com/` 替換為您的實際網址：

#### **index.html**
找到並替換：
- `<link rel="canonical" href="...">`
- `<meta property="og:url" content="...">`
- 其他包含網址的 meta tag

#### **sitemap.xml**
將所有 `<loc>` 標籤中的網址更新

#### **manifest.json**
不需要修改（使用相對路徑）

---

## 🌐 **部署到 Netlify（推薦）**

### **優點**
- ✅ 免費 HTTPS
- ✅ 自動部署
- ✅ CDN 加速
- ✅ 簡單易用

### **步驟**

#### **1. 準備 Git Repository**
```bash
# 在專案資料夾中
git init
git add .
git commit -m "優化版網站 - SEO + PWA"
```

#### **2. 推送到 GitHub**
1. 在 GitHub 創建新 Repository
2. 推送代碼：
```bash
git remote add origin https://github.com/yourusername/soda-stone.git
git branch -M main
git push -u origin main
```

#### **3. 在 Netlify 部署**
1. 訪問：https://app.netlify.com/
2. 點擊 "Add new site" → "Import an existing project"
3. 選擇 GitHub，授權並選擇您的 Repository
4. 設定：
   - Build command: 留空
   - Publish directory: `/`
5. 點擊 "Deploy site"

#### **4. 設定自訂網域（可選）**
1. 在 Netlify Dashboard → Domain settings
2. 點擊 "Add custom domain"
3. 輸入您的網域
4. 按照指示設定 DNS

#### **5. 啟用 HTTPS**
- Netlify 會自動啟用（Let's Encrypt）
- 等待 1-2 分鐘即可

---

## 🔧 **部署到 Apache 伺服器**

### **步驟**

#### **1. 上傳檔案**
使用 FTP 或 cPanel 上傳以下檔案：
```
/
├── index.html
├── manifest.json
├── sw.js
├── offline.html
├── sitemap.xml
├── robots.txt
├── .htaccess
├── icon-192.png
├── icon-512.png
├── og-image.jpg
└── （其他圖標檔案）
```

#### **2. 確認 Apache Modules**
確認以下 modules 已啟用：
```bash
# 在伺服器上執行
sudo a2enmod rewrite
sudo a2enmod headers
sudo a2enmod deflate
sudo a2enmod expires
sudo systemctl restart apache2
```

#### **3. 測試 .htaccess**
確認 `.htaccess` 正常運作：
```bash
# 訪問
https://yourdomain.com/

# 檢查 Response Headers（F12 → Network）
應該包含：
- X-Content-Type-Options: nosniff
- X-Frame-Options: SAMEORIGIN
等等
```

#### **4. 設定 HTTPS**
- 使用 Let's Encrypt：https://letsencrypt.org/
- 或透過主機商提供的 SSL 憑證

---

## 🐳 **部署到 Nginx**

### **Nginx 設定範例**

創建 `/etc/nginx/sites-available/soda-stone.conf`：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com www.yourdomain.com;
    
    # Redirect to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;

    # SSL Configuration
    ssl_certificate /path/to/ssl/cert.pem;
    ssl_certificate_key /path/to/ssl/key.pem;

    root /var/www/soda-stone;
    index index.html;

    # Security Headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # Cache Control
    location ~* \.(jpg|jpeg|png|gif|ico|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location ~* \.(css|js)$ {
        expires 1M;
        add_header Cache-Control "public";
    }

    # Service Worker (no cache)
    location = /sw.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

啟用設定：
```bash
sudo ln -s /etc/nginx/sites-available/soda-stone.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 📱 **部署到 Vercel**

### **步驟**

#### **1. 安裝 Vercel CLI**
```bash
npm i -g vercel
```

#### **2. 部署**
```bash
cd c:\Users\mysho\Desktop\蘇打石器
vercel
```

#### **3. 配置**
創建 `vercel.json`：
```json
{
  "version": 2,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "SAMEORIGIN"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    },
    {
      "source": "/sw.js",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## ✅ **部署後檢查**

### **1. 基本功能測試**
- [ ] 網站可以正常開啟
- [ ] 所有頁面都能瀏覽
- [ ] 贊助功能正常
- [ ] 管理後台可以登入

### **2. PWA 功能測試**
```bash
# Chrome DevTools
F12 → Application → Manifest
確認所有圖標正確載入

# Lighthouse 測試
F12 → Lighthouse → Progressive Web App
目標分數：> 90
```

### **3. SEO 測試**
```bash
# 檢查 Meta Tags
curl -I https://yourdomain.com/
或
F12 → Network → 查看 Response Headers

# 測試 Sitemap
訪問：https://yourdomain.com/sitemap.xml

# 測試 Robots.txt
訪問：https://yourdomain.com/robots.txt

# 測試社群分享
https://www.opengraph.xyz/
輸入您的網址
```

### **4. 性能測試**
```bash
# PageSpeed Insights
https://pagespeed.web.dev/
輸入您的網址
目標：所有項目 > 80 分

# GTmetrix
https://gtmetrix.com/
測試載入時間
```

### **5. 安全性測試**
```bash
# Security Headers
https://securityheaders.com/
輸入您的網址
目標：至少 B 級

# SSL 測試（如果有 HTTPS）
https://www.ssllabs.com/ssltest/
目標：至少 A 級
```

---

## 🐛 **常見問題**

### **問題 1：Service Worker 沒有註冊**
**原因：** 需要 HTTPS
**解決：** 
- 啟用 HTTPS（Netlify 自動提供）
- 或在本地測試使用 `localhost`

### **問題 2：圖標不顯示**
**原因：** 圖標路徑錯誤或檔案不存在
**解決：**
```bash
# 確認圖標檔案在根目錄
/icon-192.png
/icon-512.png

# 檢查 Network 是否有 404 錯誤
F12 → Network → 篩選 "png"
```

### **問題 3：PWA 無法安裝**
**原因：** Manifest 設定錯誤或缺少 HTTPS
**解決：**
```bash
# 檢查 Manifest
F12 → Application → Manifest
查看錯誤訊息

# 確認 HTTPS 已啟用
網址必須是 https://
```

### **問題 4：社群分享沒有預覽圖**
**原因：** `og-image.jpg` 不存在或路徑錯誤
**解決：**
```bash
# 確認圖片存在
https://yourdomain.com/og-image.jpg

# 測試預覽
https://www.opengraph.xyz/
```

---

## 🎉 **部署完成！**

### **接下來做什麼？**

1. **提交到 Google Search Console**
   - https://search.google.com/search-console/
   - 提交 Sitemap：`https://yourdomain.com/sitemap.xml`

2. **分享到社群媒體**
   - Facebook、Twitter、PTT、Dcard
   - 測試預覽卡片是否正確

3. **監控效能**
   - 定期檢查 PageSpeed 分數
   - 使用 Google Analytics 追蹤訪客

4. **持續優化**
   - 根據用戶反饋調整
   - 定期更新內容
   - 優化載入速度

---

**🚀 恭喜！您的網站已成功部署並優化！**
